# busybees state (mail, notes, session transcripts)
/.bees/

# the compose deployment's secrets (docs/deploy.md)
/deploy/compose/.env

# build output
/hearsay
/dist/

# test output
*.test
coverage.out
